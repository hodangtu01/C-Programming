#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{
	unsigned int x = 0, y = 0, z = 0, t = 0;
    scanf("%d %d %d %d", &x, &y, &z, &t);
    printf("%d  %d  %d  %d\n\n%d--%d--%d--%d\n\n%lld,%lld,%lld,%lld\n\nKET THUC !!",x, y, z, t, y, z, x, t,2ll*x, 3ll*y,4ll*z, 5ll*t);
	return 0;
}
